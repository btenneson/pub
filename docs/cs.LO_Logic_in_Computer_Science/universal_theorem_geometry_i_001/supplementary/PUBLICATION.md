# Universal Theorem Geometry I

Canonical subject: `cs.LO — Logic in Computer Science`

Canonical public reader: `/pub/cs.LO_Logic_in_Computer_Science/universal_theorem_geometry_i_001/`

Current status: the available archived PDF is damaged and has not passed publication validation. The public reader therefore does not offer a PDF download and does not present the damaged file as a valid publication.

The file `damaged-original.pdf` is retained only as a recovery artifact. It is not the canonical publication PDF and should not be shared as the paper.

To restore this publication, replace the damaged archive with a readable PDF, update `scripts/publications.json` with its canonical PDF path, SHA-256, page count, and a page-verified quotation, then rebuild and run `scripts/audit_subject_catalog.py` before publishing.
