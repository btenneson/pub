These historical workflows write incompatible publication pages or validate the retired /papers layout. They are preserved here but are no longer active. The single subject catalog, scripts/publications.json, now owns publication metadata; build_publications.py generates the public readers and cards. Restore a historical workflow only after porting it to that contract. Standalone archive-only PDF compilation workflows remain active.
